[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["cd backend && pip install -r requirements.txt --break-system-packages"]

[start]
cmd = "cd backend/app && gunicorn app:app --bind 0.0.0.0:$PORT"
